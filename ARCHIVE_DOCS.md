# Archived Mintlify docs

This branch restores `apps/docs` (and the monorepo wiring for `pnpm docs:dev`) on top of current `staging`.

- **Source snapshot:** `4de7dd2b` (parent of docs removal `5c9b0a3e`)
- **Submission branch:** `staging` — no `apps/docs`
- **Safe to merge into `staging`** if the assessor says docs can stay in the submission package

## Restore

Merge this PR, or:

```bash
git checkout archive/docs
# or copy apps/docs from this branch
```
