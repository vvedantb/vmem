{
  "name": "vmem",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "turbo": "cd web && pnpm turbo",
    "dev": "cd web && pnpm dev",
    "build": "cd web && pnpm build",
    "start": "cd web && pnpm start",
    "lint": "cd web && pnpm lint",
    "typecheck": "cd web && pnpm typecheck"
  }
}
