node_modules
.env.local
.mcp.json
.next
.turbo
dist
out
build
.eslintcache
*.tsbuildinfo
.tanstack

# Local agent / IDE config (not shared)
.claude/
.cursor/
.agents/

# Local project context (not shared)
FYP-PROJECT-CONTEXT.md
CLAUDE.md
AGENTS.md
CODEBASE.md
skills-lock.json
skills-lock.json.bak

# Internal docs, plans, dissertation — local only
internal/

# Agent workflow specs + local scripts — not shared
docs/superpowers/
scripts/

# Presentation deck — local/dissertation only
# apps/web/src/routes/slides.tsx
# apps/web/src/routes/_components/slides/
# apps/web/public/slides/
# Chrome extension build + live-verification scripts — local only
apps/chrome-extension/scripts/

# Oxlint report — local only
oxlint-report.json