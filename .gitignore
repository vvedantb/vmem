node_modules
.env.local
e2e/.env
e2e/.env.local
.mcp.json
.next
.turbo
dist
out
build
.eslintcache
*.tsbuildinfo
.tanstack

# Local agent / IDE config (not shared)
.claude/
.cursor/
.agents/

# Local project context (not shared)
FYP-PROJECT-CONTEXT.md
CODEBASE.md
ARCHITECTURE.md
AGENTS.md
CLAUDE.md
DESIRES.md
LEARNINGS.md
MISTAKES.md
skills-lock.json
skills-lock.json.bak

# Internal docs, plans, dissertation — local only
internal/

# Agent workflow specs + local scripts — not shared. The compiler-check pair
# is the exception: the pre-push hook runs it, so it must ship.
docs/superpowers/
scripts/*
!scripts/compiler-check.mjs
!scripts/compiler-check-baseline.json

# Chrome extension build + live-verification scripts — local only
apps/chrome-extension/scripts/

# Oxlint report — local only
oxlint-report.json

# Mintlify local preview
**/.mintlify/
e2e/.auth/
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
playwright-report/
test-results/