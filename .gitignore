node_modules
.env.local
.mcp.json
.next
.turbo
dist
out
build
.eslintcache
*.tsbuildinfo
.tanstack

# Local agent / IDE config (not shared)
.claude/
.cursor/
.agents/

# Local project context (not shared)
FYP-PROJECT-CONTEXT.md
CLAUDE.md
AGENTS.md
CODEBASE.md
skills-lock.json
skills-lock.json.bak

# Internal docs, plans, dissertation — local only
internal/

# Agent workflow specs + local scripts — not shared
docs/superpowers/
scripts/

# Presentation deck is tracked on this branch (PR #105). Do not ignore
# /slides paths here — mainline ignores them until this PR merges.

# Chrome extension build + live-verification scripts — local only
apps/chrome-extension/scripts/

# Oxlint report — local only
oxlint-report.json
