{
  "installCommand": "cd ../.. && HUSKY=0 pnpm install --frozen-lockfile --filter web...",
  "buildCommand": "cd ../.. && pnpm --filter web build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
