---
title: "Mobile (removed)"
description: "The Expo app is not in the main tree"
---

<Warning>
  `apps/mobile` is **not** on `main`. There is no `pnpm mobile` script and no
  Expo app in this checkout. The current clients are the web dashboard and the
  Chrome extension. A native app snapshot lives on the `archive/mobile` branch
  only.
</Warning>

The web app is responsive. Use [vmem-staging.vedantb.com](https://vmem-staging.vedantb.com) or `pnpm dev` in a mobile viewport rather than a separate native project.
