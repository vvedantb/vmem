# Archived mobile client

This branch restores the full Expo app at `apps/mobile` on top of current `staging`, for reference only.

- **Source snapshot:** `1ac1cb5f` (parent of mobile removal `5f24868c`)
- **Submission branch:** `staging` — no `apps/mobile`
- **Do not merge into `staging`** — reference / restore only

## Restore locally

```bash
git checkout archive/mobile
# or copy apps/mobile from this branch into your working tree
```
