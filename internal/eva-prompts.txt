2 more issues
context icon is not showing up anywhere
get rid of the local thing in the footer pls, not needed anymore










1. Refactor the settings area so it no longer uses the current `settings/page.tsx` layout. Create a settings sidebar with visible tabs for `Preferences` and `Data Controls`, and move the settings experience into that tabbed layout. Keep it consistent with the current app shell and make the default view land on `Preferences`.

2. Improve the mobile header/navbar styling. Make it feel more intentional and polished, with better spacing, hierarchy, and responsiveness. Preserve the current functionality, but make the mobile top bar look cleaner and more premium.

3. Redesign the sidebar section headers. Remove the current reveal-on-click section behavior. Each section should always show a fixed header in the format `[icon] Section Title` followed by a horizontal line extending to the end of the row, with that section's tabs always visible underneath.

4. Add a footer card above the divider called `Today's Stats`. It should show two metrics: `Memories Added` and `Memories Retrieved`. Match the existing visual style and use mock data for now.

5. Update the API logs page so the top three summary cards are larger and include simple line charts using mock data. The charts should feel like lightweight usage trend visuals rather than full analytics dashboards.

6. Rename `Logs` to `Usage` and rename the `API` section to `API Keys` throughout the UI where appropriate, so the information architecture is clearer and there is no generic `API` section label anymore.

7. Redesign the memories list view. Remove the table and modal pattern. Make the list use cards instead, where each card shows the memory title on the first row and tags on the second row. When a memory is clicked, split the page into a two-column layout and show the selected memory details in a right-hand panel. Remove the `Created` column entirely.

8. Add a reusable right-click context menu component to `packages/ui` so it can be used across the app. It should fit the existing component patterns and styling in the shared UI package.
